class Job {
  final String id;
  final String title;
  final String description;
  final String location;
  final bool canBeRemote;
  final String url;

  const Job({this.id, this.title, this.description, this.location, this.canBeRemote, this.url});
}
